import { Layout } from './components/layout'
import './App.css'

function App() {
  return (
    <Layout activeNav="课程学习">
      <section className="page-placeholder">
        <h1>我的课程</h1>
        <p>页面内容区域 — 后续页面将在此处渲染。</p>
      </section>
    </Layout>
  )
}

export default App
